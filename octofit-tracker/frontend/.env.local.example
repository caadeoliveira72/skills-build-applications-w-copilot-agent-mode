# Set the Codespace name used by the frontend to build the backend URL.
# Example value when running in GitHub Codespaces:
VITE_CODESPACE_NAME=your-codespace-name

# The frontend uses this variable to build the API host:
# https://<VITE_CODESPACE_NAME>-8000.app.github.dev/api/<resource>
